import TodoForm from "./TodoForm"
import TodoContainer from "./TodoContainer"

const Container = () => {
	return (
		<div>
			<TodoForm />
			<TodoContainer />
		</div>
	)
}

export default Container
